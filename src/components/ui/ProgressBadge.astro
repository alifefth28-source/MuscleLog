---
export interface Props {
  label: string;
  type: "success" | "warning" | "accent";
}

const { label, type } = Astro.props;

const classes = {
  success: "badge-success",
  warning: "badge-warning",
  accent: "badge-accent",
};
---

<span class:list={["badge", classes[type]]}>{label}</span>
